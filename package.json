{
  "name": "echo-app",
  "version": "1.0.0",
  "description": "Echo chat app powered by Claude API",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build-apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
